// import React from 'react'

// export default ({ data, isLoading, error }) => {
// 	if (error) {
// 		return <p>{error.message}</p>
// 	} else if (isLoading) {
// 		return <Loading />
// 	} else {
// 		return <WrappedComponent users={users} {...this.props} />
// 	}
// }
