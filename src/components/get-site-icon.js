import React from 'react'

export default (props) => {
    const IconExtractor = 'https://www.google.com/s2/favicons?domain='
  return (
    <React.Fragment>
        <img src={`${IconExtractor}${}`} alt={} />
    </React.Fragment>
      
    
  )
}
